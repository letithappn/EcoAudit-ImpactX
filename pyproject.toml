[project]
name = "ecoaudit"
version = "0.1.0"
description = "EcoAudit AI — Deterministic Carbon Calculation Engine"
requires-python = ">=3.10"
license = "MIT"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
